using System;
using System.Collections.Generic;
using System.Text;

namespace Guiter
{
    public enum Builder
    {

        Fender, Martin, Gibson, Collings, Olson, Ryan, Prs
    }
    /*
    public class Bulider {
       /*
        Note
         in the book its use the enum in java which different than c# 
         beacuse in java thers is tostring method which we can use it 
         inside enum to convert the enum varibles to string .
         in C# we cant use method inside enum but there is enum class
         which we can use to convert enum to string
            
         Note
         Here we can just use static string and give it the value of the string 
         without use enum but we i use it to show how we can use enum in c#
         
      
        public static String FENDER = Enum.GetName(typeof(Builder), Builder.Fender);
        public static String MARTIN = Enum.GetName(typeof(Builder), Builder.Martin);
        public static String GIBSON = Enum.GetName(typeof(Builder), Builder.Gibson);
        public static String COLLINGS = Enum.GetName(typeof(Builder), Builder.Collings);
        public static String OLSON = Enum.GetName(typeof(Builder), Builder.Olson);
        public static String RYAN = Enum.GetName(typeof(Builder), Builder.Ryan);
        public static String PRS = Enum.GetName(typeof(Builder), Builder.Prs);
       

    }*/

   
   
}
